# /stats:cache

Status: closed.

Added a dedicated cache-miss section for every turn and horizontal per-turn cache-usage rows with two-decimal percentages. Existing command tests pass, including cache-drop behavior and empty history.
